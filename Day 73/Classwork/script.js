const bmwE34 = {
    brand: "BMW",            // ბრენდი
    model: "E34",            // მოდელი
    year: 1995,              // გამოშვების წელი
    color: "Black",          // ფერი
    engine: "2.5L I6"        // ძრავის ტიპი
  };
  
  console.log(bmwE34.model); // ეს გამოიტანს E34


  //////////////////////////////////////////////////////////////////////////

  // 2.შექმენით ობიექტი ორი key-value წყვილი ( 2 property-თ ), პირველი key იქნება 'რიგი_0' მეორე key 'რიგი_1', პირველ value-ში შეინახეთ 5 ელემენტიანი მასივი ლუწი რიცხვების, მეორეში კენტი რიცხვების, გადაუყევით ორივე მასივს და გამოიტანეთ თითოეულის ელემენტი ერთდროულად, ( ცალ ცალკე არ გადაუყვეთ პირველს და მეორეს )

const obj = {
    row_0: [2, 4, 6, 8, 10], 
    row_1: [1, 3, 5, 7, 9]  
  };
  
  for (let i = 0; i < obj.row_0.length; i++) {
    console.log("Even: " + obj.row_0[i] + ", Odd: " + obj.row_1[i]);
  }
  
//3) შექმენით ფუნქცია რომელიც არგუმენტად იღებს ობიექტს, ამ ობიექტიდან კითხულობს ორ value-ს ( quantity და element ) ( ეს key-ების სახლეებია ), შემდეგ დააბრუნებს მასივს შესაბამისად. ( ის ელემენტი რაც მოცემული იყო იმდენჯერ ჩასმული მასივში რამდენჯერაც quantity-ში იყო მოცემული )

function createArrayFromObject(obj) {
    let result = [];
    for (let i = 0; i < obj.quantity; i++) {
      result.push(obj.element);
    }
    return result;
  }